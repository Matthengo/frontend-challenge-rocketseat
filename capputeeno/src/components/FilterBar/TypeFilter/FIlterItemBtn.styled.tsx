import styled from "styled-components";

export const FilterItemBtn = styled.button`
  color: inherit;
  font-weight: inherit;
  font-size: inherit;
  font-family: inherit;
  cursor: inherit;
  line-height: inherit;
  text-transform: inherit;
  border: none;
`