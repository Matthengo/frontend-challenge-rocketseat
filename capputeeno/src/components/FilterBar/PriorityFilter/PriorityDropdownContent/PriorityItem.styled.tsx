import styled from "styled-components";

export const PriorityItem = styled.button`
  color: var(--color-dark-one);
  border: none;
  font-family: inherit;
  font-size: 0.875rem;
  font-weight: 400;
  line-height: 22px;
  cursor: pointer;
  background-color: inherit;
`